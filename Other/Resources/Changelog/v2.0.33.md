# Release notes

## Changes

- an api-server which runs in the test-run-mode binds the loopback-interface instead of every ip, even when
  ListenOnEveryIP is set. Binding every ip made the operating-system ask whether the process is allowed to communicate
  in the network (the firewall-dialog of windows), which nobody can answer in an unattended test-run
